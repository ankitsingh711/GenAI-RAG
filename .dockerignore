# Keep the build context small and the image clean.
# (The index is rebuilt inside the image, so never ship the local one.)
.git
.venv
chroma_db/
__pycache__/
**/__pycache__/
*.pyc
.env
.env.example

# The frontend is deployed separately (Vercel) — not part of the backend image.
web/

docs/
